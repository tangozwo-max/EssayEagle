# Dependencies
node_modules/
.pnp
.pnp.*

# Build output
.next/
out/
build/
dist/
coverage/

# Env & secrets
.env
.env.*
*.pem
*.key

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS / editor / OneDrive cruft
.DS_Store
Thumbs.db
desktop.ini

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude local settings (machine-specific)
.claude/settings.local.json

# ── Curated repo: track ONLY the gui app + research reference notes ──
# Everything below is kept local-only (not pushed to GitHub):
/Team_folder/
/tools/
/projects/
/_upgrade/
/framework/
/.claude/
/controlling.json
/ahmed_check.txt
/bqa_extract.txt
/researchgate-requests.html
/CLAUDE.md

# ── Projects ARE published, but only their text/markdown ──
# Track handover docs, dashboards, state, and the research wiki (all .md/.json).
# Keep heavy source material (PDFs etc.) local-only — referenced by path, not committed.
/01 projects/**/*.pdf
/01 projects/**/*.docx
/01 projects/**/*.doc
/01 projects/**/*.pptx
/01 projects/**/*.png
/01 projects/**/*.jpg
/01 projects/**/*.jpeg
/01 projects/**/*.zip
/01 projects/**/*.xlsm
/01 projects/**/*.xlsx
